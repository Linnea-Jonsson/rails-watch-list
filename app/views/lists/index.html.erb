<div class="container">
<h1>Lists<h2>

<p><%= link_to "Add a list", new_list_path %></p>
<br>

<div>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
</div>

<br>
<p><%= cl_image_tag("kffjz17cnfa1ceyfpe6i",
      width: 400, height: 300, crop: :fill) %></p>

</div>
